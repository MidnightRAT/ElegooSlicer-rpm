diff --git a/deps/CGAL/CGAL.cmake b/deps/CGAL/CGAL.cmake
index 4ac6b56..0d52b9e 100644
--- a/deps/CGAL/CGAL.cmake
+++ b/deps/CGAL/CGAL.cmake
@@ -10,7 +10,7 @@ elegooslicer_add_cmake_project(
     URL      https://github.com/CGAL/cgal/archive/refs/tags/v5.4.zip
     URL_HASH SHA256=d7605e0a5a5ca17da7547592f6f6e4a59430a0bc861948974254d0de43eab4c0
     PATCH_COMMAND git apply ${CGAL_DIRECTORY_FLAG} --verbose --ignore-space-change --whitespace=fix ${CMAKE_CURRENT_LIST_DIR}/0001-clang19.patch
-    DEPENDS dep_Boost dep_GMP dep_MPFR
+    DEPENDS dep_Boost
 )
 
 include(GNUInstallDirs)
diff --git a/deps/CMakeLists.txt b/deps/CMakeLists.txt
index 87bb7d7..eca2dcc 100644
--- a/deps/CMakeLists.txt
+++ b/deps/CMakeLists.txt
@@ -47,6 +47,7 @@ endif ()
 set(DESTDIR "${CMAKE_CURRENT_BINARY_DIR}/destdir" CACHE PATH "Destination directory")
 set(DEP_DOWNLOAD_DIR ${CMAKE_CURRENT_SOURCE_DIR}/DL_CACHE CACHE PATH "Path for downloaded source packages.")
 set(FLATPAK FALSE CACHE BOOL "Toggles various build settings for flatpak, like /usr/local in DESTDIR or not building wxwidgets")
+option(USE_SYSTEM_LIBS "Use system -devel packages instead of bundled deps" OFF)
 if (NOT FLATPAK)
     set(DESTDIR "${DESTDIR}/usr/local/")
 endif()
@@ -145,7 +146,7 @@ if (NOT IS_CROSS_COMPILE OR NOT APPLE)
         CMAKE_ARGS
             -DCMAKE_INSTALL_PREFIX:STRING=${DESTDIR}
             -DCMAKE_MODULE_PATH:STRING=${PROJECT_SOURCE_DIR}/../cmake/modules
-            -DCMAKE_PREFIX_PATH:STRING=${DESTDIR}
+            -DCMAKE_PREFIX_PATH:STRING=/usr;${DESTDIR}
             -DCMAKE_DEBUG_POSTFIX:STRING=d
             -DCMAKE_C_COMPILER:STRING=${CMAKE_C_COMPILER}
             -DCMAKE_CXX_COMPILER:STRING=${CMAKE_CXX_COMPILER}
@@ -174,7 +175,7 @@ elseif(FLATPAK)
         CMAKE_ARGS
             -DCMAKE_INSTALL_PREFIX:STRING=${DESTDIR}
             -DCMAKE_MODULE_PATH:STRING=${PROJECT_SOURCE_DIR}/../cmake/modules
-            -DCMAKE_PREFIX_PATH:STRING=${DESTDIR}
+            -DCMAKE_PREFIX_PATH:STRING=/usr;${DESTDIR}
             -DCMAKE_DEBUG_POSTFIX:STRING=d
             -DCMAKE_C_COMPILER:STRING=${CMAKE_C_COMPILER}
             -DCMAKE_CXX_COMPILER:STRING=${CMAKE_CXX_COMPILER}
@@ -201,7 +202,7 @@ else()
         ${_gen}
         CMAKE_ARGS
             -DCMAKE_INSTALL_PREFIX:STRING=${DESTDIR}
-            -DCMAKE_PREFIX_PATH:STRING=${DESTDIR}
+            -DCMAKE_PREFIX_PATH:STRING=/usr;${DESTDIR}
             -DBUILD_SHARED_LIBS:BOOL=OFF
             ${_cmake_osx_arch}
             "${_configs_line}"
@@ -268,6 +269,30 @@ if(FLATPAK)
     find_package(CURL)
 endif()
 
+# When USE_SYSTEM_LIBS is on, find system packages for the dependencies
+# that can be provided by Fedora -devel packages.
+if(USE_SYSTEM_LIBS)
+    message(STATUS "USE_SYSTEM_LIBS: ON — will use system -devel packages where available")
+    find_package(ZLIB)
+    find_package(PNG)
+    find_package(EXPAT)
+    find_package(CURL)
+    find_package(JPEG)
+    find_package(Freetype)
+    if(FREETYPE_FOUND OR Freetype_FOUND)
+        set(FREETYPE_FOUND TRUE)
+    endif()
+    find_package(TBB)
+    find_package(NLopt)
+    find_package(cereal)
+    find_package(GLEW)
+    find_package(PkgConfig)
+    if(PKG_CONFIG_FOUND)
+        pkg_check_modules(BLOSC blosc)
+        pkg_check_modules(GMP gmp)
+    endif()
+endif()
+
 set(ZLIB_PKG "")
 if (NOT ZLIB_FOUND) 
     include(ZLIB/ZLIB.cmake)
@@ -289,24 +314,62 @@ set(DEP_Boost_COMPONENTS system iostreams filesystem thread log locale regex dat
 include(Boost/Boost.cmake)
 
 # The order of includes respects the dependencies between libraries
-include(Cereal/Cereal.cmake)
+set(CEREAL_PKG "")
+if(USE_SYSTEM_LIBS AND cereal_FOUND)
+    message(STATUS "Using system Cereal")
+else()
+    include(Cereal/Cereal.cmake)
+    set(CEREAL_PKG dep_Cereal)
+endif()
 include(Qhull/Qhull.cmake)
-include(GLEW/GLEW.cmake)
+if(USE_SYSTEM_LIBS AND GLEW_FOUND)
+    message(STATUS "Using system GLEW")
+else()
+    include(GLEW/GLEW.cmake)
+endif()
 
 include(GLFW/GLFW.cmake)
 include(OpenCSG/OpenCSG.cmake)
 
-include(TBB/TBB.cmake)
+set(TBB_PKG "")
+if(USE_SYSTEM_LIBS AND TBB_FOUND)
+    message(STATUS "Using system TBB")
+else()
+    include(TBB/TBB.cmake)
+    set(TBB_PKG dep_TBB)
+endif()
 
-include(Blosc/Blosc.cmake)
+if(USE_SYSTEM_LIBS AND BLOSC_FOUND)
+    message(STATUS "Using system Blosc")
+else()
+    include(Blosc/Blosc.cmake)
+endif()
+# Always build bundled OpenEXR - system OpenEXR 3.x doesn't have IlmBase::Half
+# which is required by the bundled FindOpenVDB.cmake module
 include(OpenEXR/OpenEXR.cmake)
+# Always build bundled OpenVDB - system OpenVDB cmake module has compatibility issues
 include(OpenVDB/OpenVDB.cmake)
 
-include(GMP/GMP.cmake)
-include(MPFR/MPFR.cmake)
+if(USE_SYSTEM_LIBS AND GMP_FOUND)
+    message(STATUS "Using system GMP")
+else()
+    include(GMP/GMP.cmake)
+endif()
+if(USE_SYSTEM_LIBS)
+    message(STATUS "Using system MPFR")
+else()
+    include(MPFR/MPFR.cmake)
+endif()
+# Always build bundled CGAL - system CGAL 6.x has breaking API changes
 include(CGAL/CGAL.cmake)
 
-include(NLopt/NLopt.cmake)
+set(NLOPT_PKG "")
+if(USE_SYSTEM_LIBS AND NLopt_FOUND)
+    message(STATUS "Using system NLopt")
+else()
+    include(NLopt/NLopt.cmake)
+    set(NLOPT_PKG dep_NLopt)
+endif()
 include(libnoise/libnoise.cmake)
 
 # I *think* 1.1 is used for *just* md5 hashing?
@@ -323,7 +386,7 @@ endif()
 # so, just don't even bother 
 # ...i think this is how it works? change if wrong
 set(CURL_PKG "")
-if (NOT OPENSSL_FOUND OR NOT CURL_FOUND)
+if (NOT CURL_FOUND)
     include(CURL/CURL.cmake)
     set(CURL_PKG dep_CURL)
 endif ()
@@ -373,16 +436,17 @@ include(elegoolink/elegoolink.cmake)
 
 set(_dep_list
     dep_Boost
-    dep_TBB
+    ${TBB_PKG}
     ${OPENSSL_PKG}
     ${CURL_PKG}
     ${WXWIDGETS_PKG})
 list(APPEND _dep_list dep_Sentry)
 list(APPEND _dep_list
-    dep_Cereal
-    dep_NLopt
+    ${CEREAL_PKG}
+    ${NLOPT_PKG}
     dep_OpenVDB
     dep_OpenCSG
+    dep_OpenEXR
     dep_OpenCV
     dep_CGAL
     dep_GLFW
diff --git a/deps/OpenCSG/CMakeLists.txt.in b/deps/OpenCSG/CMakeLists.txt.in
index 37da0f7..b732483 100644
--- a/deps/OpenCSG/CMakeLists.txt.in
+++ b/deps/OpenCSG/CMakeLists.txt.in
@@ -2,9 +2,8 @@ cmake_minimum_required(VERSION 3.0)
 
 project(OpenCSG)
 
-if (NOT BUILD_SHARED_LIBS)
-  set(GLEW_USE_STATIC_LIBS ON)
-elseif (MSVC)
+set(GLEW_USE_STATIC_LIBS OFF)
+if (MSVC)
   set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON)
 endif()
 
diff --git a/deps/OpenCSG/OpenCSG.cmake b/deps/OpenCSG/OpenCSG.cmake
index 52e6a94..733006c 100644
--- a/deps/OpenCSG/OpenCSG.cmake
+++ b/deps/OpenCSG/OpenCSG.cmake
@@ -5,7 +5,6 @@ elegooslicer_add_cmake_project(OpenCSG
     URL https://github.com/floriankirsch/OpenCSG/archive/refs/tags/opencsg-1-4-2-release.zip
     URL_HASH SHA256=51afe0db79af8386e2027d56d685177135581e0ee82ade9d7f2caff8deab5ec5
     PATCH_COMMAND ${CMAKE_COMMAND} -E copy ${CMAKE_CURRENT_LIST_DIR}/CMakeLists.txt.in ./CMakeLists.txt
-    DEPENDS dep_GLEW
 )
 
 if (TARGET ${ZLIB_PKG})
diff --git a/deps/OpenCV/OpenCV.cmake b/deps/OpenCV/OpenCV.cmake
index 77681d2..0ea1f4d 100644
--- a/deps/OpenCV/OpenCV.cmake
+++ b/deps/OpenCV/OpenCV.cmake
@@ -21,12 +21,12 @@ elegooslicer_add_cmake_project(OpenCV
        -DBUILD_EXAMPLES=OFF
        -DBUILD_JASPER=OFF
        -DBUILD_JAVA=OFF
-       -DBUILD_JPEG=ON
+       -DBUILD_JPEG=OFF
        -DBUILD_APPS_LIST=version
        -DBUILD_opencv_apps=OFF
        -DBUILD_opencv_java=OFF
        -DBUILD_OPENEXR=OFF
-       -DBUILD_PNG=ON
+       -DBUILD_PNG=OFF
        -DBUILD_TBB=OFF
        -DBUILD_WEBP=OFF
        -DBUILD_ZLIB=OFF
diff --git a/deps/OpenVDB/OpenVDB.cmake b/deps/OpenVDB/OpenVDB.cmake
index 489e250..47b915e 100644
--- a/deps/OpenVDB/OpenVDB.cmake
+++ b/deps/OpenVDB/OpenVDB.cmake
@@ -15,10 +15,11 @@ elegooslicer_add_cmake_project(OpenVDB
     URL https://github.com/tamasmeszaros/openvdb/archive/a68fd58d0e2b85f01adeb8b13d7555183ab10aa5.zip 
     URL_HASH SHA256=f353e7b99bd0cbfc27ac9082de51acf32a8bc0b3e21ff9661ecca6f205ec1d81
     PATCH_COMMAND git apply ${OPENVDB_DIRECTORY_FLAG} --verbose --ignore-space-change --whitespace=fix ${CMAKE_CURRENT_LIST_DIR}/0001-clang19.patch
-    DEPENDS dep_TBB dep_Blosc dep_OpenEXR dep_Boost
+    DEPENDS dep_OpenEXR dep_Boost
     FORCE_RELEASE_CONFIG
     CMAKE_ARGS
-        -DCMAKE_POSITION_INDEPENDENT_CODE=ON 
+        -DCMAKE_POSITION_INDEPENDENT_CODE=ON
+        -DCMAKE_MODULE_PATH=/usr/lib64/cmake/OpenVDB
         -DOPENVDB_BUILD_PYTHON_MODULE=OFF
         -DUSE_BLOSC=ON
         -DOPENVDB_CORE_SHARED=${_build_shared} 
diff --git a/deps/elegoolink/elegoolink.cmake b/deps/elegoolink/elegoolink.cmake
index 490d10e..0443721 100644
--- a/deps/elegoolink/elegoolink.cmake
+++ b/deps/elegoolink/elegoolink.cmake
@@ -42,7 +42,7 @@ elegooslicer_add_cmake_project(elegoolink
     DEPENDS 
         dep_ixwebsocket 
         dep_PahoMqttCpp
-        dep_CURL
+        ${CURL_PKG}
         ${OPENSSL_PKG}
 
     CMAKE_ARGS 
